"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.validators = void 0;
const support_1 = require("@appium/support");
const MAX_URL_ERROR_LENGTH = 100;
exports.validators = {
    setUrl: (url) => {
        if (!URL.canParse(url)) {
            throw new Error(`'${support_1.util.truncateString(String(url), { length: MAX_URL_ERROR_LENGTH })}' must be a valid URL`);
        }
    },
    setNetworkConnection: (type) => {
        if (!isNumber(type) || [0, 1, 2, 4, 6].indexOf(type) === -1) {
            throw new Error('Network type must be one of 0, 1, 2, 4, 6');
        }
    },
};
function isNumber(o) {
    return typeof o === 'number' || !Number.isNaN(parseInt(o, 10)) || !Number.isNaN(parseFloat(o));
}
//# sourceMappingURL=validators.js.map