import path from 'node:path';
export function relative(from, to) {
    if (to === undefined) {
        return (nextTo) => `.${path.sep}${path.relative(from, nextTo)}`;
    }
    return `.${path.sep}${path.relative(from, to)}`;
}
//# sourceMappingURL=path.js.map